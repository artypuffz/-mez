// Starting resource values before any background modifier is applied.
// Not final balancing — revisited once real weekly simulation exists.
// Gameplay Expansion Part A — health starts near-full (a fresh comez
// hasn't been worn down yet), social starts at a neutral midpoint (not a
// claim about the character's pre-residency social life either way).
export const DEFAULT_RESOURCES = {
  stress: 20,
  fatigue: 15,
  burnout: 0,
  health: 100,
  social: 50,
  money: 12000,
};
